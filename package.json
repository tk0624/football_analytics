{
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "export": "next export",
  "deploy": "npm run build && npm run export && npm run postexport",
  "postexport": 
  "echo > docs/.nojekyll"
}
}