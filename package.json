{
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "deploy": "npm run build && npm run postexport",
  "postexport": "node -e \"require('fs').closeSync(require('fs').openSync('docs/.nojekyll','w'))\""
}
}